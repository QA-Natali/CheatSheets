# Python Cheat Sheet

## TOC  

## Комментарии  
```py
# - однострочный комментарий
```
```py
'''
В кавычках можно писать многострочный комментари
'''
```
## Примитивные типы данных и операторы  

### Числа и математика  

Целые  числа (int)

3

Вещественные числа ( Числа с плавающей запятой) (float)  

0.1

**Математика**

```py
+ сложение
- вычитание
* умножение
/ деление
// целочисленное деление
% остаток от деления
** возведение в степень
( ) для повышения приоритета операций
```
### Булевые значения

Ключевые слова всегда пишутся с заглавной буквы  
True - истина  
False - ложь

Для отрицания используется ключеое слово not  

not True - ложь  
not False - истина  

Булевые операторы

and - оператор И - истина, когда два условия верно  
or -  оператор ИЛИ -  истина, когда одно условие верно  

Операторы сравнения  
```py
!= - не равно  
== -  равно  
> - больше  
<= - меньше  
>= - больше или равно  
<= - меньше или равно  
```
Сравнение можно писать цепочкой  
```py
1 < 2 > 3 # True  
2 > 6 > 2 # False  
```

### Тип None  

Для обозначения пустоты используется значение None  

Для того, чтобы проерить является ли что-то равное None используйте ключевое слово 'is'.  
Оно похоже на '==', но больше подходит для пользовательских типов даных.  
```py
# None, 0 и пустые строки/ списки/ словари/ кортежи приводятся к False.  
# Все остальные значения равны  True.  
bool(0) #=>False
bool('') #=>False
bool([]) #=>False
bool({}) #=>False
bool(()) #=>False
```

### Тип Строка

Для того, чтобы создать переменную типа строка,  
её значение нужно взять в одинарныее или двойные кавычки.  
```py
"Привет, мир!"  
'Привет, мир!'  
```
Строки можно складывать с помощью оператора '+' и если строки  
не содержат переменных, то их можно складыать без оператора '+'.  
```py
'Привет,' + 'мир!'  
'Привет,' 'мир!'  
```
Для определиня длины строки используйте оператор len()  

## Переменные и коллекции

### Переменные 

В переменной хранятся данные, которые потом обрабатываются.  

```py
myName = 'Natalie'
my_favorite_number = 100
myFirstSentenciece = 'Hello World!'
float_number = 3.1415926
myList = ['Shopen', 'Bethoven', ..., 'Mozart']
```
### Список

```py
something = [1, 'Word', None, True,..., '67']
```

Списки хранят последовательности данных и являются  
упорядоченной коллекцией.

**Операции неменяющие множества**

|            Метод           |   Что делает   |
|----------------------------|----------------|
|list.appen(x)               |Добавляет элемент в конец списка |
|list.extend(L)              |Расширяет список list, добавляет в конец все элементы списка L |
|list.instert(i,x)           |Вставляет на i-ый элемент значение x  |
|list.remove(x)              |Удаляет первый элемент в списке, имеющий значение X. ValueError, если такого элемента не существует |
|list.pop([i])               |Удаляет i-ый элемент и возращает его. Если индекс не указан, удаляется последний элемент. |
|list.index(x[,start[,end]]) |Возвращает положение перого элемента со значением x (при этом поиск ведется от start до end)|
|list.count(x)               |Возвращает количество элементов со значением X |
|list.sort([key=функция])    |Сортирует список на основе функций |
|list.reverse()              |Разорачивает список |
|list.copy()                 |Поверхностная копия списка |
|list.clear()                |Очищает список |

### Тип кортежи (tuple)  

Кортежи похожи на списки, которые нельзя изменять.  

```py
tup = (1, 2, 3)
``` 

Все операции над списками, не изменяющие список  
(сложение, умножение на число, методы index() и count() и некоторые другие операции).  
Можно также по-разному менять элементы местами и так далее.  

Важно помнить, что для создания кортежа из одного элемента, после него  
нужно поставить запятую, в любом другом случае это не обязательно делать.  

```py
tup = ('s', )

# или даже так  

tup = 's',
```
**Операции меняющие множества**  

| Метод | Что делает |
|-------|------------|
| set.update(other, ...); set \|= other \| | объединение |
| set.intersection_update(other, ...); set &= other & | пересечение |
| set.difference_update(other, ...); set -= other \| | вычитание |
| set.symmetric_difference_update(other); set ^= other | множество из элементов, встречающихся в одном множестве, но не встречающиеся в обоих |
| set.add(elem) | добавляет элемент в множество |
| set.remove(elem) | удаляет элемент из множества. KeyError, если такого элемента не существует |
| set.discard(elem) | удаляет элемент, если он находится в множестве |
| set.pop() | удаляет первый элемент из множества. Так как множества не упорядочены, нельзя точно сказать, какой элемент будет первым |
| set.clear() | очистка множества |

### Тип frozenset  

Множество, которое обладает свойствами кортежа. Можно делать все неизменяемые операции множеств.  

## Тип Словари

Словари в Python - неупорядоченные коллекции произвольных объектов с доступом по ключу.  
Их иногда ещё называют ассоциативными массивами или хеш-таблицами.  

**Как создать словарь:**  

Объявляем переменную, в которой хранится словарь 

```py
filled_dict = {'dict': 1, 'dictionary': 2}

empty_dict = {}   # Пустой словарь  
```

С помощью функции dict:

```py
filled_dict = dict(short='dict', long='dictionary')

number_dict = dict([(1, 1), (2, 4)])
```

С помощью метода fromkeys:

```py
filled_dict = dict.fromkeys(['a', 'b']) # {'a': None, 'b': None}

yet_dict = dict.fromkeys(['a', 'b'], 100) # {'a': 100, 'b': 100}
```

C помощью генераторов словарей  

```py
filled_dict = {a: a ** 2 for a in range(7)} # {0: 0, 1: 1, 2: 4, 3: 9, 4: 16, 5: 25, 6: 36}
```

**Методы работы со словарём**

|                 Метод                 | Что делает |
|---------------------------------------|------------|
|dict.clear()                           |Очищает словарь|
|dict.copy()                            |Возвращает копию словаря|
|classmethod dict.fromkeys(seq[, value])|Создает словарь с ключами из seq и значением value (по умолчанию None)|
|dict.get(key[, default])               |Возвращает значение ключа, но если его нет, не бросает исключение, а возвращает default (по умолчанию None)|
|dict.items()                           |Возвращает пары (ключ, значение)|
|dict.keys()                            |Возвращает ключи в словаре|
|dict.pop(key[, default])               |Удаляет ключ и возвращает значение. Если ключа нет, возвращает default (по умолчанию бросает исключение)|
|dict.popitem()                         |Удаляет и возвращает пару (ключ, значение). Если словарь пуст, бросает исключение KeyError. Помните, что словари неупорядочены|
|dict.setdefault(key[, default])        |Возвращает значение ключа, но если его нет, не бросает исключение, а создает ключ со значением default (по умолчанию None)|
|dict.update([other])                   |Обновляет словарь, добавляя пары (ключ, значение) из other. Существующие ключи перезаписываются. Возвращает None (не новый словарь!)|
|dict.values()                          |Возвращает значения в словаре|
